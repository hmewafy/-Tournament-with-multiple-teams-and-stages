# -Tournament-with-multiple-teams-and-stages
This project simulates a sports tournament from group stage to knockout rounds. It creates teams, schedules and simulates matches across stadiums, tracks results and standings, allocates budgets, optimizes stadium logistics with a minimal-cost network, and determines the final champion
